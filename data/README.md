# Data Directory

This repository distinguishes unrestricted synthetic fixtures from restricted external datasets.

- `synthetic/` contains generated demonstration data used by unrestricted experiments.
- `private/` is Git-ignored and reserved for locally authorized external datasets.

No file in `synthetic/` should be confused with SWaT or any other operational dataset. Synthetic fixtures are labeled explicitly.
